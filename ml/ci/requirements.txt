# CI training + gate deps. Extends ml/requirements.txt.
-r ../requirements.txt

# Experiment tracking
mlflow>=2.10

# Kaggle GPU orchestration (sleep model is offloaded to a Kaggle T4 kernel;
# the runner only pushes/polls/downloads — no torch needed on the runner).
# >=1.8 supports the new API tokens (KAGGLE_API_TOKEN); still reads the legacy
# ~/.kaggle/kaggle.json (username+key) that kaggle_run.py writes.
kaggle>=1.8

# Figures (confusion/ROC/feature-importance/SHAP) for CPU models on the runner
shap>=0.44

# Data/experiment versioning — feature CSVs are DVC-tracked on the DagsHub remote
dvc[s3]>=3.0
